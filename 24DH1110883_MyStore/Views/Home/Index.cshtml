@model _24DH1110883_MyStore.Models.ViewModel.HomeProductVM
@{
    ViewBag.Title = "Customer Home Page";
    <link rel="stylesheet" type="text/css" href="~/Content/myStyle/CustomerHome.css" />
}

@* --- PHẦN BANNER --- *@
<section class="container-fluid banner">
    <div class="banner-container text-center banner-bg">
        <div class="banner-overlay">
            <h2>Chào mừng đến trang TEACHPHONE</h2>
            <p>Mua sắm trực tuyến với hàng ngàn sản phẩm đa dạng và chất lượng</p>
            <a href="#" class="btnBanner">Bắt đầu mua sắm</a>
        </div>
    </div>
</section>

@* --- PHẦN TÌM KIẾM SẢN PHẨM --- *@


<section class="container-fluid searchProd">
    <h2 class="text-center" style="margin-top: 20px;">Tìm kiếm sản phẩm</h2>

    @using (Html.BeginForm("Index", "Home", FormMethod.Get))
    {
        <div class="form-group search-container search-wrapper">
            @Html.TextBoxFor(m => m.SearchTerm, new
            {
                @class = "search-input form-control search-input-custom", // Thêm class custom
                placeholder = "Nhập tên, mô tả hoặc chủng loại sản phẩm"
            })

            <button type="submit" class="search-button btn btn-primary">Tìm kiếm</button>
        </div>
    }
</section>

<hr />

@* PHẦN SẢN PHẨM NỔI BẬT *@
@Html.Partial("_PVFeatureProduct", Model)

@* PHẦN SẢN PHẨM MỚI *@
@Html.Partial("_PVNewProduct", Model)